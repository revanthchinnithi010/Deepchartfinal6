# DeepChart Final Project Structure

This archive is already flattened for direct GitHub repository upload.

The repository root contains `package.json`, `pnpm-workspace.yaml`, `artifacts/`, `lib/`, and the Railway/Replit configuration files.

Do **not** upload this archive as a nested ZIP inside the repository. Extract it and upload the contents of this directory to the repository root.

## Railway

For the frontend service, use the repository root and the existing workspace configuration.
For the API service, deploy `artifacts/api-server` as the service root if your Railway service is configured with a Root Directory, or use the repository workspace command defined in the project configuration.
