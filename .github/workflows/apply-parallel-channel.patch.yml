name: Apply Parallel Channel Patch
on:
  push:
    branches: [main]
permissions:
  contents: write
jobs:
  patch:
    if: contains(github.event.head_commit.message, '[apply-channel-patch]')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Patch channel implementation
        run: python3 scripts/fix_parallel_channel_v4.py
      - name: Commit patched implementation
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add artifacts/trading-journal/src/components/charts/DrawingOverlay.tsx
          git diff --cached --quiet && exit 0
          git commit -m 'fix: make parallel channel TradingView-compatible'
          git push
      - name: Remove patch tooling
        run: |
          rm -f .github/workflows/apply-parallel-channel.patch.yml scripts/fix_parallel_channel_v4.py .github/workflows/apply-channel-v4.yml
          git add -A
          git diff --cached --quiet && exit 0
          git commit -m 'chore: remove temporary channel patch tooling'
          git push
